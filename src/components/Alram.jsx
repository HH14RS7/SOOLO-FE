import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';

export const Alram = ({ newNotice, setnewNotice }) => {
  // const navigate = useNavigate();

  // useEffect(() => {
  //   if (newNotice.alarmType !== undefined) {
  //     setTimeout(() => {
  //       setnewNotice({});
  //     }, 3000);
  //   }
  // }, [newNotice]);

  // const clickMove = () => {
  //   if (newNotice.alarmType === "partyDetail") {}
  // }

  return <div>Alram</div>;
};
